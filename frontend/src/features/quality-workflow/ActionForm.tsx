import { useState } from 'react'
import { useQualityCase } from '../../quality-workflow/QualityCaseContext'

export function ActionForm({ actor }: { actor: string }) { const { addAction } = useQualityCase(); const [title, setTitle] = useState('Zvýšit podíl projektově orientovaných úloh v klíčových předmětech.'); const [description, setDescription] = useState('Promítnout projektové úlohy do tří klíčových předmětů.'); const [responsible, setResponsible] = useState('Garant SP'); const [dueDate, setDueDate] = useState('30. 6. 2027'); return <form className="workflow-form" onSubmit={(event) => { event.preventDefault(); addAction({ title, description, responsible, dueDate }, actor) }}><h3>Nové opatření</h3><label>Název opatření<input value={title} onChange={(event) => setTitle(event.target.value)} /></label><label>Popis<textarea value={description} onChange={(event) => setDescription(event.target.value)} /></label><label>Odpovědnost<input value={responsible} onChange={(event) => setResponsible(event.target.value)} /></label><label>Termín<input value={dueDate} onChange={(event) => setDueDate(event.target.value)} /></label><button className="primary-button" type="submit">Navrhnout opatření</button></form> }