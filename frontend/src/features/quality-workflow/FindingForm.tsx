import { useState } from 'react'
import { useQualityCase } from '../../quality-workflow/QualityCaseContext'

export function FindingForm({ actor }: { actor: string }) { const { addFinding } = useQualityCase(); const [title, setTitle] = useState('Vyšší podíl projektové výuky'); const [description, setDescription] = useState('Studenti požadují vyšší podíl projektově orientovaných úloh v klíčových předmětech.'); return <form className="workflow-form" onSubmit={(event) => { event.preventDefault(); addFinding({ type: 'IMPROVEMENT_OPPORTUNITY', title, description }, actor) }}><h3>Nové zjištění</h3><label>Typ zjištění<select value="IMPROVEMENT_OPPORTUNITY" disabled><option>Příležitost ke zlepšení</option></select></label><label>Název<input value={title} onChange={(event) => setTitle(event.target.value)} /></label><label>Popis<textarea value={description} onChange={(event) => setDescription(event.target.value)} /></label><button className="primary-button" type="submit">Vytvořit zjištění</button></form> }