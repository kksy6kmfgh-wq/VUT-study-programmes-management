import { useState } from 'react'
import { useQualityCase } from '../../quality-workflow/QualityCaseContext'

export function VerificationForm({ actor }: { actor: string }) { const { addVerification } = useQualityCase(); const [implementation, setImplementation] = useState(true); const [effectiveness, setEffectiveness] = useState(true); const [comment, setComment] = useState('Projektově orientované úlohy byly zavedeny do tří klíčových předmětů a studentská zpětná vazba se zlepšila.'); return <form className="workflow-form" onSubmit={(event) => { event.preventDefault(); addVerification({ verifier: actor, verifiedAt: '2027-09-15', implementationVerified: implementation, effectivenessVerified: effectiveness, comment }) }}><h3>Ověření opatření</h3><label><input type="checkbox" checked={implementation} onChange={(event) => setImplementation(event.target.checked)} /> Implementace ověřena</label><label><input type="checkbox" checked={effectiveness} onChange={(event) => setEffectiveness(event.target.checked)} /> Účinnost ověřena</label><label>Komentář<textarea value={comment} onChange={(event) => setComment(event.target.value)} /></label><button className="primary-button" type="submit">Uložit ověření</button></form> }