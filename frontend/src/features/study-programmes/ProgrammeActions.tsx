import type { ActionRecord, StudyProgramme } from '../../types/studyProgramme'

export function ProgrammeActions({ programme }: { programme: StudyProgramme }) { return <section className="section-panel"><div className="section-panel-header"><div><span className="eyebrow">QUALITY LOOP</span><h2>Opatření</h2><span className="section-caption">Opatření napříč hodnocením kvality a dalšími procesy</span></div><span className="section-caption">{programme.actions.length} záznamy</span></div><div className="action-list">{programme.actions.map((action) => <ActionRow action={action} key={action.title} />)}</div><div className="action-note"><strong>ACTION COMPLETED</strong> není totéž jako <strong>QUALITY LOOP CLOSED</strong>. Uzavření vyžaduje samostatné ověření účinnosti.</div></section> }

function ActionRow({ action }: { action: ActionRecord }) { return <article className="action-row"><div className="action-title"><strong>{action.title}</strong><span>{action.sourceFinding}</span></div><div className="action-cell"><small>Responsible</small>{action.responsible}</div><div className={`action-cell ${action.status === 'OVERDUE' ? 'overdue' : ''}`}><small>Due date</small>{action.dueDate}</div><div className="action-state"><span className={`action-status action-${action.status.toLowerCase()}`}>{action.status}</span>{action.status === 'COMPLETED' && <span className="completion-label">ACTION COMPLETED</span>}</div><div className={`verification-state verification-${action.verification.toLowerCase()}`}><small>Verification</small>{action.verification === 'VERIFIED' ? 'VERIFIED · CLOSED' : 'planned'}</div></article> }