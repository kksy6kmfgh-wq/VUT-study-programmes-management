import { useState } from 'react'
import { evidenceRegister } from '../../content/methodologyCatalog'

export function ProgrammeEvidenceRegister() {
  const [selectedId, setSelectedId] = useState(evidenceRegister[0]?.id ?? '')
  const selected = evidenceRegister.find((item) => item.id === selectedId)
  return <div className="programme-evidence-layout"><section className="section-panel"><div className="section-panel-header"><div><span className="eyebrow">EVIDENCE REGISTER</span><h2>Registr důkazů programu</h2><span className="section-caption">Jeden důkaz lze opakovaně použít v G, H, ročním hodnocení i dalších procesech.</span></div><button type="button" className="primary-button">+ Nový důkaz</button></div><div className="programme-evidence-table"><div className="programme-evidence-head"><span>ID</span><span>Důkaz</span><span>Vlastník</span><span>Verze / datum</span><span>Síla</span><span>Použití</span></div>{evidenceRegister.map((item) => <button type="button" className={selectedId === item.id ? 'selected' : ''} onClick={() => setSelectedId(item.id)} key={item.id}><strong>{item.id}</strong><span>{item.title}<small>{item.reference}</small></span><span>{item.owner}</span><span>{item.date}</span><b>{item.strength}</b><span>{item.usedBy}</span></button>)}</div></section>{selected && <aside className="section-panel evidence-detail"><span className="eyebrow">DETAIL DŮKAZU</span><h2>{selected.title}</h2><dl><div><dt>ID</dt><dd>{selected.id}</dd></div><div><dt>Vlastník</dt><dd>{selected.owner}</dd></div><div><dt>Aktuální verze</dt><dd>{selected.date}</dd></div><div><dt>Síla důkazu</dt><dd>{selected.strength}</dd></div></dl><h3>Použito v</h3><p>{selected.usedBy}</p><h3>Historie verzí</h3><div className="evidence-version-list"><span><strong>v3</strong> aktuální</span><span><strong>v2</strong> archivní</span><span><strong>v1</strong> archivní</span></div><p className="section-caption">Starší hodnocení zůstávají navázána na verzi důkazu, která byla platná v době hodnocení.</p></aside>}</div>
}
