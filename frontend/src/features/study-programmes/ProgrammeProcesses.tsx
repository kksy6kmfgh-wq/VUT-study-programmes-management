import type { StudyProgramme } from '../../types/studyProgramme'

export function ProgrammeProcesses({ programme }: { programme: StudyProgramme }) {
  const stages = ['Zahájeno', 'Sebehodnocení garanta', 'Kontrola úplnosti', 'Hodnocení PS', 'Vypořádání', 'Podklad RVH', 'Rozhodnutí']
  return <div className="programme-process-layout"><section className="section-panel"><div className="section-panel-header"><div><span className="eyebrow">WORKFLOW</span><h2>Periodické hodnocení 2028</h2><span className="section-caption">Řízený proces s termíny, úkoly a redakční komunikací.</span></div><span className="process-state-label">Probíhá</span></div><div className="programme-process-timeline">{stages.map((stage, index) => <div className={index === 1 ? 'current' : index === 0 ? 'done' : ''} key={stage}><span>{index === 0 ? '✓' : index + 1}</span><strong>{stage}</strong><small>{index === 1 ? 'termín 30. 9. 2028' : index === 0 ? '15. 8. 2028' : 'čeká'}</small></div>)}</div><div className="process-history-table"><div><strong>Roční hodnocení 2027</strong><span>Probíhá</span><span>15. 12. 2027</span></div><div><strong>Reakreditace 2025/2026</strong><span>Uzavřeno</span><span>{programme.accreditationProcesses[0]?.decision.decidedAt ?? '—'}</span></div><div><strong>Změna programu 2026/2</strong><span>Uzavřeno</span><span>1. 9. 2026</span></div></div></section><aside className="section-panel process-my-tasks"><span className="eyebrow">VAŠE ÚKOLY</span><h2>2 aktivní</h2><div><strong>G27 · doplnit důkaz</strong><span>do 30. 9.</span></div><div><strong>D014 · aktualizovat</strong><span>do 30. 9.</span></div><p className="section-caption">Termíny a požadavky může redakčně řídit pracovník kvality.</p></aside></div>
}
