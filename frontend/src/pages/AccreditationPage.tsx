import { accreditationPortfolio } from '../mock/workspace'
import { approvalRegimeLabels, processTypeLabels, statusLabels } from '../presentation/labels'

export function AccreditationPage() { return <WorkspacePage eyebrow="PROCESNÍ PORTFOLIO" title="Akreditace" description="Přehled schvalovacích a akreditačních procesů napříč studijními programy."><div className="filter-row"><button className="filter active" type="button">Všechny procesy</button><button className="filter" type="button">Čeká na rozhodnutí</button><button className="filter" type="button">Externí NAÚ</button></div><div className="panel portfolio-table"><div className="portfolio-table-head accreditation-grid"><span>Studijní program</span><span>Verze</span><span>Typ procesu</span><span>Režim</span><span>Stav procesu</span><span>Aktuální krok</span><span>Termín</span><span>Rozhodnutí</span></div>{accreditationPortfolio.map((item) => <div className="portfolio-row accreditation-grid" key={item.code + item.version}><div><strong>{item.programme}</strong><span>{item.code}</span></div><span>{item.version}</span><span>{processTypeLabels[item.processType]}</span><span>{approvalRegimeLabels[item.regime]}</span><span><b className={`table-state state-${item.status.toLowerCase()}`}>{statusLabels[item.status] ?? item.status}</b></span><span>{item.step}<small>{item.responsible}</small></span><span>{item.due}</span><span className={item.decision === 'Žádné rozhodnutí' ? 'muted-cell' : 'decision-cell'}>{item.decision}</span></div>)}</div><div className="status-separation"><strong>PROCESS STATUS != DECISION</strong><span>Stav procesu popisuje postup případu. Rozhodnutí je samostatný formální výsledek autorizovaného orgánu.</span></div></WorkspacePage> }

function WorkspacePage({ eyebrow, title, description, children }: { eyebrow: string; title: string; description: string; children: React.ReactNode }) { return <div className="content-frame workspace-page"><div className="page-heading"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p className="page-description">{description}</p></div></div>{children}</div> }