import { useState } from 'react'
import { councilSections, councilTables } from '../content/programmeCouncil'

export function ProgrammeCouncilPage() { const [selected, setSelected] = useState(0); const table = councilTables[selected]; return <div className="content-frame council-page"><div className="page-heading"><div><span className="eyebrow">RADA STUDIJNÍHO PROGRAMU · N-PI</span><h1>Roční hodnocení SP</h1><p className="page-description">Zápis z jednání rady programu: data, interpretace, rizika a plán zlepšování.</p></div></div><div className="council-layout"><aside className="council-nav">{councilSections.map((item, index) => <button type="button" className={selected === index ? 'active' : ''} onClick={() => setSelected(index)} key={item}>{String.fromCharCode(65 + index)}. {item}</button>)}<div className="council-table-nav"><strong>Tabulky 1–18</strong>{councilTables.map((item, index) => <button type="button" className={selected === index ? 'active' : ''} onClick={() => setSelected(index)} key={item.number}>Tab. {item.number} · {item.title}</button>)}</div></aside><main className="council-content"><div className="council-header-card"><span>Projednáno dne</span><strong>12. 12. 2027</strong><span>Garant SP</span><strong>Pavel Lošák</strong><span>Zdroj</span><strong>Dokument B · zápis rady SP</strong></div><section className="council-table-card"><span className="eyebrow">TABULKA {table.number}</span><h2>{table.title}</h2><p className="section-caption">{table.purpose}</p><div className="source-table-wrap"><table><thead><tr>{table.columns.map((column) => <th key={column}>{column}</th>)}</tr></thead><tbody>{table.rows.map((row, rowIndex) => <tr key={rowIndex}>{row.map((cell, cellIndex) => <td key={cellIndex}>{cell}</td>)}</tr>)}</tbody></table></div>{table.number >= 3 && table.number <= 15 && <div className="council-comments"><div><span>DATA Z IS / AUTOMATICKY DODANÁ DATA</span><p>Údaje jsou vstupem pro jednání, nikoli závěrem.</p></div><div><span>KOMENTÁŘ GARANTA</span><p>{table.comment}</p></div><div><span>ZÁVĚR / KOMENTÁŘ RADY SP</span><p>{table.conclusion}</p></div></div>}</section><section className="pdca-panel"><strong>PDCA návaznost</strong><span>předchozí opatření</span><b>→</b><span>měřený dopad</span><b>→</b><span>nová data</span><b>→</b><span>interpretace</span><b>→</b><span>problém / riziko</span><b>→</b><span>nové opatření</span><b>→</b><span>KPI</span></section><section className="council-conclusion"><h2>Závěr rady programu</h2><div className="summary-grid"><div><span>Hlavní silné stránky</span><strong>Dobrá připravenost absolventů na SZZ; stabilní výsledky výuky.</strong></div><div><span>Hlavní témata ke zlepšení</span><strong>Průchodnost ve 2. ročníku; internacionalizace; kapacita garantů.</strong></div><div><span>Identifikovaná rizika</span><strong>Poptávka, personální nástupnictví, omezená kapacita opatření.</strong></div><div><span>Priority dalšího roku</span><strong>Bariéry průchodnosti ve 2. ročníku a projektová výuka.</strong></div></div></section></main></div></div> }
