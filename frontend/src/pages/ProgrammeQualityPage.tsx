import { useState } from 'react'
import { qualityPortfolio } from '../mock/workspace'

export function ProgrammeQualityPage() { const [filter, setFilter] = useState('all'); const items = qualityPortfolio.filter((item) => filter === 'all' || filter === 'attention' && (item.findings > 0 || item.actions > 0) || filter === 'upcoming' && item.nextReview === '2027' || filter === 'findings' && item.findings > 0 || filter === 'evidence' && item.needsEvidence); return <WorkspacePage eyebrow="SHARED QUALITY CORE" title="Kvalita SP" description="Přehled pravidelných hodnocení a jejich otevřených navazujících kroků napříč programy."><div className="filter-row"><FilterButton active={filter === 'all'} onClick={() => setFilter('all')}>Vše</FilterButton><FilterButton active={filter === 'attention'} onClick={() => setFilter('attention')}>Vyžaduje pozornost</FilterButton><FilterButton active={filter === 'upcoming'} onClick={() => setFilter('upcoming')}>V příštích 12 měsících</FilterButton><FilterButton active={filter === 'findings'} onClick={() => setFilter('findings')}>Otevřená zjištění</FilterButton><FilterButton active={filter === 'evidence'} onClick={() => setFilter('evidence')}>Chybějící důkazy</FilterButton></div><div className="panel portfolio-table"><div className="portfolio-table-head quality-grid"><span>Studijní program</span><span>Poslední hodnocení</span><span>Další hodnocení</span><span>Požadavky doloženy</span><span>Otevřená zjištění</span><span>Otevřená opatření</span><span>Stav review</span></div>{items.map((item) => <div className="portfolio-row quality-grid" key={item.code}><div><strong>{item.programme}</strong><span>{item.code}</span></div><span>{item.lastReview}</span><span>{item.nextReview}</span><span className={item.needsEvidence ? 'warning-cell' : ''}>{item.evidence}</span><span>{item.findings}</span><span>{item.actions}</span><span><b className="table-state state-review">{item.state}</b></span></div>)}</div><p className="page-footnote">Přehled slouží k řízení kvality a prioritizaci práce. Neřadí programy na „dobré“ nebo „špatné“.</p></WorkspacePage> }
function FilterButton({ active, onClick, children }: { active: boolean; onClick: () => void; children: React.ReactNode }) { return <button className={`filter ${active ? 'active' : ''}`} type="button" onClick={onClick}>{children}</button> }
function WorkspacePage({ eyebrow, title, description, children }: { eyebrow: string; title: string; description: string; children: React.ReactNode }) { return <div className="content-frame workspace-page"><div className="page-heading"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p className="page-description">{description}</p></div></div>{children}</div> }